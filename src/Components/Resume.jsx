import { Box, Button } from "@chakra-ui/react";

export const Resume=()=>{
    return (
        <Box id='resume'>
            <Button>Download Resume</Button>
            <Button>See My resume</Button>
        </Box>
    )
};