import {Box, Input} from '@chakra-ui/react'

export const Contact=()=>{
    return (
        <Box id='contact'>
        <Input/>
        <Input/>
        <label>First Name</label>
        </Box>
    )
};